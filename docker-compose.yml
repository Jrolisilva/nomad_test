services:
  api:
    build: .
    container_name: nomad_test_api
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - ./data:/app/data
      - /app/node_modules
    environment:
      - DATABASE_URL=file:./data/dev.db
    command: npm run start:dev
